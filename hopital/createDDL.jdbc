CREATE TABLE SERVICE (SERVNO BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, SERVLOCALISATION VARCHAR(50) NOT NULL, SERVNAME VARCHAR(50) NOT NULL UNIQUE, manager BIGINT, PRIMARY KEY (SERVNO))
CREATE TABLE MEDECIN (MEDENO BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, MEDENAME VARCHAR(50) NOT NULL, MEDEFIRSTNAME VARCHAR(50) NOT NULL, MEDESALARY FLOAT NOT NULL, CHEF BIGINT, service BIGINT, PRIMARY KEY (MEDENO))
ALTER TABLE MEDECIN ADD CONSTRAINT UNQ_MEDECIN_0 UNIQUE (MEDENAME, MEDEFIRSTNAME)
ALTER TABLE SERVICE ADD CONSTRAINT FK_SERVICE_manager FOREIGN KEY (manager) REFERENCES MEDECIN (MEDENO)
ALTER TABLE MEDECIN ADD CONSTRAINT FK_MEDECIN_service FOREIGN KEY (service) REFERENCES SERVICE (SERVNO)
ALTER TABLE MEDECIN ADD CONSTRAINT FK_MEDECIN_CHEF FOREIGN KEY (CHEF) REFERENCES MEDECIN (MEDENO)
